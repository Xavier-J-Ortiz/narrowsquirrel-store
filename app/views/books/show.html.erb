<div class="large-3 small-12 column">
	<%=image_tag @book.poster_url%>
</div>

<div class="large-9 small-12 column">
	<h3>
		<%= @book.title %>
		(<%= @book.publish_date %>)
		<%=image_tag @book.poster_url%>
	</h3>
	<p class="label book-label radius mb1">$ <%= @book.price %></p>

	<p><%= @book.description %></p>
</div>
